import { Routes } from '@angular/router';

// Portfólio de página única (single page). As seções são navegadas via âncoras (#sobre, #projetos, etc).
// Rotas adicionais podem ser incluídas aqui futuramente (ex: página de detalhes de projeto).
export const routes: Routes = [];
